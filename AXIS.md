# アクシズカプセル / Axis Capsule

日付: 2026-09-10
系統: AXIOM
銘板: アクシズカプセル（EN: Axis Capsule）
核の個体名は「基準体」。系統名と混ぜない。
位置づけ: 系譜の次形。min3 でも金型でもない。知能は入れない。

## 分割

```
Axis0  α rules + β 制約世界観     Hash-A0   観察から書かない
Axis1  制約参照 + エピソード      Hash-B1   γindex / Δindex / IS / γ不変
Axis2  制約参照 + η回帰 + 差異修正力         提案のみ。核へ書かない
Axis3  思考フレーム 1+?=0                  不全。analogy は gap のみ
Axis4  LLM窓口                             推測独自性 / 演技性。昇格しない
Persona  Hash-P                            名前は β と一致必須
```

Axis1〜4 と Persona は Axis0 の Hash-A0 を親にする。
系統名は束ねの銘板。A0 には入らない。

## 実行

```bash
python3 axiom_axis.py
python3 axiom_axis.py demo
python3 axiom_axis.py analog0
```

境界トークンは切断面。開いている穴だけ使える。使い切ったら落ちる。
