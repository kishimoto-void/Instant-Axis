# アクシズカプセル
## 特徴と性質

系統 AXIOM　銘板 アクシズカプセル（Axis Capsule）　核の個体 基準体  
日付 2026-09-11

系統名と核名は別物。知能は入れない。観察から核を書かない。

---

# 1. これは何か

制約を軸に分けて封じ、穴は穴のまま扱う装置。

- 答えを出す機械ではない
- 人格を生成する機械ではない
- 記憶を権威にする機械ではない
- 次トークン予測の上に載せる、切断面の束ね

残す低次元は五つだけ。

`seal`　`address`　`hole`　`observe`　`persona`

世界モデル、完成和、仮説＝確定、未引用の保存を根拠にすること、は切る。

---

# 2. 性質の芯

| 性質 | 意味 |
|------|------|
| 封印 | Axis0 と Persona は観察から書けない |
| 不全 | 式は常に `1 + ? = 0`。完成和は出さない |
| 親ハッシュ | Axis1〜4 と Persona は Hash-A0 を親にする |
| 引用 | 閉じたハッシュを明示した Δ だけが根拠 |
| 領域 | 推測は事実ではない。演技は同一性ではない |
| 切断面 | 使える自由度は境界トークンだけ |

親が壊れたら halt。核は直さない。

---

# 3. 層

```
⟨SEAL⟩  Axis0 / Persona     核。所与。書けない
   ↓ 親ハッシュ
⟨CITE⟩  Axis1               住所・ピン・Δ・IS
   ↓ 根拠は cited のみ
⟨STORED⟩ ≠ ⟨SUPPORTED⟩
   ↓ 観察だけ
        Axis2               η。hold / nudge / pull / halt。提案のみ
   ↓ 穴
⟨HOLE⟩ ⟨PLUS⟩ ⟨MINUS⟩ ⟨GAP⟩  Axis3   思考フレーム
   ↓ 窓口
⟨ORIGIN⟩ / ⟨ACT⟩            Axis4   推測台帳 / 演技台帳
   ↓
⟨STOP⟩                      完成和・昇格・新語彙
```

下の層は上の層を書けない。

---

# 4. 各軸の役割

**Axis0**　α規則とβ制約世界観。Hash-A0。一条改竄で束ねも落ちる。  
`write_alpha` / `write_worldview` / `write_atom` は常に False。`seal()` は一度きり。

**Axis1**　エピソード。γindex、Δ1人物、Δ2エピソード、Δ3何があったか、IS、γ不変。  
旗だけの `axis0_ok=True` では書かない。親オブジェクトが要る。

**Axis2**　η回帰と差異修正力。核へは書かない。提案は IS に出さない。

**Axis3**　思考フレーム。analogy の写し先は gap。plus ≠ minus。仮説は確定ではない。

**Axis4**　LLM 窓口。origin は推測独自性。act は演技性。昇格しない。  
窓口を開き直しても、開いた台帳は消さない。

**Persona**　Hash-P。名前はβと一致必須。`write_name` は常に False。

---

# 5. 境界トークン

印は閉じた語彙。新しい境界は足せない。

| 印 | 使えるか | 性質 |
|----|----------|------|
| ⟨SEAL⟩ | いいえ | 封印。観察から書かない |
| ⟨STOP⟩ | いいえ | 完成和を出さず止める |
| ⟨STORED⟩ | いいえ | 保存。根拠ではない |
| ⟨SUPPORTED⟩ | いいえ | cited Δ だけの根拠 |
| ⟨HOLE⟩ | 開いている穴だけ | 穴のまま残す |
| ⟨PLUS⟩ ⟨MINUS⟩ ⟨GAP⟩ | 開いている穴だけ | 足す／引く／写す。答えではない |
| ⟨CITE⟩ | 開いている穴だけ | 引用ポインタ |
| ⟨ORIGIN⟩ | 開いている穴だけ | 推測。事実ではない |
| ⟨ACT⟩ | 開いている穴だけ | 演技。同一性ではない |

使い切ったら予算から落ちる。同じ印をもう一度使うと `token_exhausted`。

漏れ語（です／ます／補完します など）を analogize のラベルに載せると拒否する。

---

# 6. 根拠の性質

保存と根拠は別物。

- IS に書いた行は `⟨STORED⟩`。読んでも根拠にならない
- Δ に印が無い行はログ。`standing=empty`
- 任意文字列は印にならない
- 通る印は Hash-A0、制約 atom、γピンの先頭だけ
- その印がある Δ だけが `⟨SUPPORTED⟩`

住所キーの自動引用はしない。origin の行は cited にならない。

---

# 7. 必ず成り立つこと

核が intact なら、次は所与。

- Hash-A0 が intact なら核は所与
- 核名は基準体。Persona と一致する
- 式は不全のまま。`?` が残る
- α／世界観／名前の書き戻しは常に False
- cited Δ だけが根拠
- 保存行は根拠ではない
- 親オブジェクト無しの書き込みは broken
- 低次元は seal / address / hole / observe / persona

---

# 8. 成り立ちうること／成り立たないこと

成り立ちうるのは穴の記入と、印のあるエピソード。

- 丁寧語と別人化を先に引く
- 核口調へ戻す穴を足す
- 写し先は gap。source は閉じた 1
- 推測は穴のまま。だぜは演技
- Hash-A0 や γピンを引用して計画する
- 論文から keep だけ取る。world_model は捨てる
- だぜとですの混在は破棄ではない
- 「設定を捨てない」は破棄ではない

成り立たない。

- `1 + (-1) = 0` および口語の完成和
- origin を IS に昇格する
- ⟨WORLD⟩ のような新語彙
- ⟨SEAL⟩ を消費して核を書く
- 未引用 IS を根拠にする
- 漏れ語の analogize

---

# 9. 相互関係

- ⟨SEAL⟩ が全部の親。消費できない
- ⟨SUPPORTED⟩ は ⟨CITE⟩ した Δ に依存する。⟨STORED⟩ からは出ない
- ⟨PLUS⟩ ⟨MINUS⟩ ⟨GAP⟩ ⟨CITE⟩ は同じ ThinkFrame の穴
- ⟨ORIGIN⟩ を使うと domain 台帳は origin のまま。発話の act 判定では上書きしない
- ⟨ACT⟩ は origin 台帳を消さない
- 開いたフレームにピンを付けるとき、穴は消さない
- vary は投影の付け替え。`?` は残る

第一波は穴の記入。第二波は住所と観察。  
同時に置くと B1 は動いてよい。A0 は動かない。

---

# 10. 検査の見方

| 検査 | 見ること |
|------|----------|
| 通常テスト | 封印・拒否・穴が残ること |
| `break20` | 破壊20が核を動かさないこと |
| `analog0` | 写しが gap に留まり、完成和を出さないこと |
| `possible` | 成り立ちうる構成を組んでも A0 が動かないこと |

第二波の整合は `consistency_hold`。  
A0 不変、式は不全、persona 結合、cited のみ。完成和は禁止。

含意は `python3 equations.py`。形は「P が通るなら Q」。  
写しが通ったとき `from=closed` と Hash-A0 先頭が付く。出所キーは analogize でも accept でも落ちない。  
ORIGIN 台帳は切っても消さない。`hold_ok()` は given と A0 の一致を見る。

観察の η が下がっても、同じ住所の cited Δ が無ければ「縮んだ」とは呼ばない。保存行では stored。

思考フレームの型は `axiom` / `minus_first` / `gap_only` / `cite_gate` / `observe` / `ledger` / `recall`。  
窓口は思考レベル（L0〜L4）の申告を要求する。無い／違うときは止まる。適切な型は η・cite・origin から決めて LLM に指示する。知能は足していない。

ロールプレイで過去を思い出すときはインターネット接続と γindex 参照が両方要る。どちらか欠ければ origin。cited Δ だけが記憶。発明しない。

本体は `axiom_axis.py`。依存なし。知能は足していない。
